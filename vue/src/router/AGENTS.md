# Router

本目录拥有路径、懒加载组件、路由元数据和访问控制。

## Local Rules

- 路径、参数名和 redirect query 是外部导航合同；修改时检查所有链接和调用方。
- 认证判断复用 store/client 的会话事实，不复制 token 解析。
- 页面组件保持懒加载，除非有测量证明需要改变。
- 守卫必须避免重定向循环，并保留用户原目标。

## Local Verification

运行路由文件定向 lint；行为变化添加守卫/导航测试或记录未覆盖风险。
